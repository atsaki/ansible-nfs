machine:
  ruby:
    version: 2.0.0-p481
  python:
    version: 2.7.9
  environment:
    KITCHEN_DRIVER: digitalocean

test:
  post:
    - bundle exec kitchen test --destroy=always --concurrency=4:
        timeout: 1200

